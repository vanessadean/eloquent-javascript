//// forEach ////

    // forEach(array, action) {
    //   for (var i = 0; i < array.length; i++)
    //     action(array[i]);
    // }

    // .forEach(function(x) {
    //  do something to each item (x) in the array
    // });


//// filter ////

    // filter(array, test) {
    //   var passed = [];
    //   for (var i = 0; i < array.length; i++) {
    //     if (test(array[i]))
    //       passed.push(array[i]);
    //   }
    //   return passed;
    // }

    // .filter(function(x) {
    //   add item (x) to a new array if it meets a condition specified here;
    // });


//// map ////

    // function map(array, transform) {
    //   var mapped = [];
    //   for (var i = 0; i < array.length; i++)
    //     mapped.push(transform(array[i]));
    //   return mapped;
    // }

    // // .map(function(x) {
    //   creates a new array with each item (x) transformed in some way;
    // });


//// reduce ////
    // function reduce(array, combine, start) {
    //   var current = start;
    //   for (var i = 0; i < array.length; i++)
    //     current = combine(current, array[i]);
    //   return current;
    // }

    // .reduce(function(a,b){
    //    starts with value c and combines or compares with a and b values in specified manner
    // }, c); 
    // c is an optional argument - will take first value in the array as default start


